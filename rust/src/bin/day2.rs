// UNFINISHED

use std::collections::HashMap;

fn main() {
    let input = include_str!("../../../input/day2.txt");

    let mut scores = HashMap::from([("A X", 1 + 3)]);

    println!("{}", input);
}
